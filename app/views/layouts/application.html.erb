<!DOCTYPE html>
<html>
  <head>
    <title>JournalAppRailsScaffold</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <!-- CSS only -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-gH2yIJqKdNHPEq0n4Mqa/HGKIhSkIHeL5AyhkYV8i59U5AR6csBvApHHNl/vI1Bx" crossorigin="anonymous">
  </head>

  <body>
    <div class="container-fluid">
      <header class="d-flex flex-wrap justify-content-center py-3 mb-4 border-bottom">
        <a href="/" class="d-flex align-items-center mb-3 mb-md-0 me-md-auto text-dark text-decoration-none">
          <svg class="bi me-2" width="40" height="32"><use xlink:href="#bootstrap"></use></svg>
          <span class="fs-4">Journal App</span>
        </a>

        <ul class="nav nav-pills">
          <% if user_signed_in? %>
            <li class="nav-item">
              <%= link_to "Categories", root_path, {class: "nav-link"} %>
            </li>
            <li class="nav-item">
              <%= link_to "Tasks Today", root_path, {class: "nav-link"} %>
            </li>
            <li class="nav-item">
            <a class="nav-link disabled" href="#"><%= current_user.email %> </a>
            </li>
            <li class="nav-item">
              <%= button_to "Log out", destroy_user_session_path, {method: :delete, class: "btn btn-danger"} %>
            </li>
          <% else %>
              <li class="nav-item">
                <%= link_to "Log in", new_user_session_path, {class: "btn btn-outline-primary", role: "button"} %>
              </li>
              <li class="nav-item">
                <%= link_to "Sign up", new_user_registration_path, {class: "nav-link" , role: "button"} %>
              </li>
          <% end %>
        </ul>
      </header>
    </div>

    <div class="container-fluid">
      <%= yield %>
    </div>
    
    

    <!-- JavaScript Bundle with Popper -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/js/bootstrap.bundle.min.js" integrity="sha384-A3rJD856KowSb7dwlZdYEkO39Gagi7vIsF0jrRAoQmDKKtQBHUuLZ9AsSv4jD4Xa" crossorigin="anonymous"></script>
  </body>
</html>
